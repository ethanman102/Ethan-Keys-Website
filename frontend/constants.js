const apiURL = 'http://localhost:8000/';

const pathNames = {
    blog : "Blog",
    projects : "Projects",
    games : "Games",
    home : "Home"
}

const mode = {
    EDIT: 'E',
    VIEW: 'V'
}

const toolTypes = {
    FRONTEND: 'FRONTEND',
    BACKEND: 'BACKEND'
}

const SIZE = 5

export {apiURL,pathNames,mode,toolTypes, SIZE}